# Local development environment settings
# When running with npm start, React will use these values

# Direct connection to gateway during local development
# This avoids CORS issues when running npm start
REACT_APP_API_BASE_URL=http://localhost:8081/api